## Agent skills

### Issue tracker

Issues and PRDs for this repo are tracked in GitHub Issues via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

This repo uses a single-context domain doc layout: root `CONTEXT.md` plus root `docs/adr/` for ADRs. See `docs/agents/domain.md`.
